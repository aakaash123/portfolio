import React from 'react';
import './Background.css';  // Assuming the CSS above is saved in this file

const Background = () => {
    return <div className="background"></div>;
};

export default Background;
